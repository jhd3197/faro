[package]
name = "faro-agentd"
version = "1.3.30"
edition = "2021"
license = "MIT"
description = "Faro remote-agent daemon — run on a machine to control it from Faro over an encrypted, paired connection."

[[bin]]
name = "faro-agentd"
path = "src/main.rs"

[dependencies]
faro-agent-proto = { path = "../faro-agent-proto" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
base64 = "0.22"
dirs = "5"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
gethostname = "0.4"
mdns-sd = "0.11"

[dev-dependencies]
tempfile = "3"
