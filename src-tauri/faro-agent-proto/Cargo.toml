[package]
name = "faro-agent-proto"
version = "1.3.18"
edition = "2021"
license = "MIT"
description = "Wire protocol + encrypted channel for Faro's remote-agent (Faro-to-Faro) bridge."

# Deliberately Tauri-free and dependency-light so `faro-agentd` can link it
# without pulling in the GUI stack (and the compile-time `../dist` check).
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
thiserror = "1"
tokio = { version = "1", features = ["io-util", "net", "macros", "rt", "time"] }
snow = "0.9"
base64 = "0.22"
sha2 = "0.10"
rand = "0.8"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
